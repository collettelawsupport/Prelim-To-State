[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "24"
  # These public configuration values intentionally appear in client output,
  # documentation, or tests and are not credentials.
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SITE_URL,QBO_ENVIRONMENT,BIG_FORM_URL,QBO_REDIRECT_URI,EMAIL_FROM,GMAIL_USER"
  # These user-supplied binary assets contain no application credentials. Use
  # filename substrings so bundled and published copies are omitted as well.
  SECRETS_SCAN_OMIT_PATHS = "handbook-texas-state-beauty-2026.pdf,texas-olm-candyland-background.jpg"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."reconcile-qbo-payments"]
  included_files = ["netlify/assets/handbook-texas-state-beauty-2026.pdf"]

[functions."quickbooks-webhook"]
  included_files = ["netlify/assets/handbook-texas-state-beauty-2026.pdf"]

[functions."resend-big-form"]
  included_files = ["netlify/assets/handbook-texas-state-beauty-2026.pdf"]

[functions."submit-registration"]
  included_files = ["netlify/assets/handbook-texas-state-beauty-2026.pdf"]

# Keep the two public registration experiences separate while serving both from
# this one deployment. The Honor Roll domain must be assigned to this Netlify
# project as a domain alias before this rewrite becomes active.
[[redirects]]
  from = "https://honorrollregistration.texasourlittlemiss.net/"
  to = "/honor-roll/"
  status = 200
  force = true

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'"

[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
